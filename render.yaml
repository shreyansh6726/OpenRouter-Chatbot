services:
  - type: web
    name: openrouter-chatbot-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: OPEN_ROUTER_API
        sync: false
      - key: CORS_ORIGINS
        sync: false